PORT=5001
NODE_ENV=development

# 데이터베이스 설정
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=new_secure_password_123!
DB_NAME=movie_review_db

# JWT 설정 (새로운 보안 키)
JWT_SECRET=completely_new_super_secure_random_key_2024_xyz789!@#$%^&*()

# 파일 업로드 경로
UPLOAD_PATH=./uploads

# CORS 설정
FRONTEND_URL=http://localhost:3000
